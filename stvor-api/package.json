{
  "name": "@stvor/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "STVOR_DEV=1 bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "keygen": "bun scripts/keygen.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target bun --minify"
  },
  "dependencies": {
    "@stvor/core": "workspace:*",
    "fastify": "^5.2.1",
    "@fastify/cors": "^10.0.2",
    "@fastify/rate-limit": "^10.2.2",
    "zod": "^3.24.1",
    "nanoid": "^5.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
